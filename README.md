# trisoul_x

DSH 的通用单模型插件。保留原 trisoul 的分层提示词、三官行为原文、画布式上下文与分层记忆；通用工具、技能、会话和按需子代理复用宿主。

当前适配 **DSH 0.1.5-alpha.1**（2026-09-08 发布）。npm 的 `latest` 标签当时仍指向 0.1.2-rc.1，因此依赖固定到已发布的新版本。

## 运行

需要 Node.js ≥22.19、pnpm。

```sh
pnpm install
pnpm build
pnpm start
```

默认端口 **3083**，`PORT` 可改。使用终端打印的登录链接打开 DSH。主模型在 DSH 的“设置 → 模型”和对话模型选项中配置；插件的后台模型、记忆范围和画布参数在“设置 → trisoul_x”中配置。

本项目启动器使用独立 `data/dsh` 目录和 `trisoul-x` profile。`DSH_HOME` 可指定其他数据目录。首次启动使用 DSH 官方 CLI 创建 web profile 并安装本地插件，不修改原 trisoul 的配置。

已有 DSH 0.1.5-alpha.1 的用户也可在自己选择的 profile 中安装：

```sh
dsh plugin --profile YOUR_PROFILE add link:/absolute/path/to/trisoul_x
```

安装前先在源码目录运行 `pnpm build`，安装后重启宿主。该 bundle 将默认 Agent preset 设为 trisoul-x，并按本项目约定配置完整访问和无需审批。

## 使用

- **记忆范围**：与原 trisoul 同位置，位于对话输入区工具行左侧。首次发消息前可选完全版、项目级、会话级；发出后绑定该会话。设置中的默认范围只影响未绑定的新会话。
- **工作上下文**：按任务、工作状态、记忆文档分标签查看；需求原文和验证结果在任务内展开，笔记、纪要与探针补记按需展开。
- **记忆**：当前会话或整个记忆库，支持内容/项目/层级筛选、编辑、版本恢复、批量删除与恢复、注入及召回记录、分片状态与版本永久删除。
- **自动记忆整理**：按 global / cross / project 分片，由 overlap/conflict、已有记忆实际变更、空闲轮巡或可选批次频率触发。恢复片内游标、跨项目晋升候选、用户来源与使用记录；会话私有层不参与长期整理，项目级会话仅处理本项目。
- **执行监控**：按概览、调用记录、上下文分标签查看。概览显示总用量、缓存、调用与耗时；轨迹节点可点击查看调用详情，组件读数与后台统计按需展开。输入区显示当前上下文和运行统计。上下文历史从升级后的新请求开始记录。
- **设置**：采用蓝色系，分为“常用 / 高级”；后台模型可跟随主模型、统一或分别配置；恢复原三档压缩频率和自定义值，以及记忆消化/补注/轮巡、状态、探针、旧用户消息退役和可选上限。保存后下一步/下一批生效；主模型与普通工具仍使用 DSH 自带设置。
- **任务与验证**：`todo_write` 合并原 `task_map` 与 `todo`，负责原文摘录、必需锚点、按项编辑、删除和勾选；独立 `verify_link` 负责关联证据、执行测试、撤销和查看。两者共用稳定任务/证据 ID 和同一份记录；编辑任务仍自动取消完成并清除旧证据。主模型收尾时依次检查未完成任务、缺少合格证据和仅文字证据，按原文提醒后继续执行；文字证据复核成功后记为已问过，新挂接的证据重新复核。以 DSH `todo/write` 事件持久化，原生清单和工作上下文面板共用数据，页面仍在任务下展示对应证据，继续对话仍可查看。
- **通用工具**：文件读写和搜索、Shell、后台作业、网页、技能、按需子代理、目标和工作流由 DSH 提供。细节沿用宿主“轨迹”、设置和工具卡片。
- **记忆与画布**：消化、语义检索、独立状态提炼、任务记忆文档换代、阶段结束放行、压缩底稿、检查点合并及事实探针均已恢复。默认保留用户原话、最新状态/任务与近期事件；可配置旧用户消息退役。`recall` 支持语义记忆检索和按序号回捞原文，探针遗漏事实进入后续纪要或即时补记。

`web_search` 需要宿主搜索服务的有效配置；主对话模型的 Ark 密钥不等于搜索凭据。MCP 由 DSH 宿主配置，本轮未连接额外 MCP 服务。

## 代码

一个包，一份插件 UI 构建；会话与模型执行循环全部由 DSH 承担。

| 位置 | 职责 |
| --- | --- |
| `cordis.patch.yml` / `presets/` | 宿主安装补丁与 Agent 组合 |
| `src/index.mjs` / `src/dsh-agent.mjs` | 插件接入、事件、设置和普通扩展工具 |
| `src/todolist.mjs` | 原版摘录、锚点、任务状态、验证链接与测试运行 |
| `src/tasks.mjs` | 原任务操作、勾选与验证入口的合并 |
| `src/prompts.mjs` | 原文融合及必要接口适配 |
| `src/hub.mjs` / `src/hub-store.mjs` | 记忆消化/分片整理、范围、版本、使用记录与监控 |
| `src/memory-context.mjs` / `src/state-zone.mjs` | 语义检索、任务补注、文档换代与独立状态提炼 |
| `src/probe.mjs` / `src/effort.mjs` | 压缩事实探针、补记、后台推理档位能力适配 |
| `src/canvas.mjs` | DSH V3 上下文区间整理 |
| `src/client/` | 输入区选择器、设置与右侧面板 |
| `scripts/` | UI 构建与独立 profile 启动 |

`data/` 已忽略。首次使用独立启动器时，可承接本目录中早期原型的模型配置和记忆；原型对话日志继续保留在 `data/sessions/`，未转换成 DSH 会话。新对话由 DSH 保存。原 trisoul 项目及数据未改动。

## 提示词与测试

[提示词逐项对照及改动原因](PROMPT_CHANGES.md)，[工作约定](AGENTS.md)，[上游许可](THIRD_PARTY_NOTICES.md)。

`pnpm test` 使用临时目录、本地模拟模型和真实 DSH profile，不调用付费模型。覆盖原文保留、范围绑定与隔离、原文锚点与歧义消解、按项操作及编辑联动、真实验证运行与取消、记忆版本和失败回退、原生工具与项目指令/技能目录、V3 整理与原文回捞、统一任务记录和跨轮恢复。另已使用用户授权的 DeepSeek v4 flash 完成真实文件、技能、子代理和记忆后台联调。

当前共 49 项自动测试通过，包含真实 DSH 的后台设置热更新、状态与记忆分工、探针、快照清理、版本管理和回捞。用户此前审定的提示词删除项与命令安全门排除要求继续有效；完整恢复/适配理由见提示词对照表 P59–P70。

本轮进一步对齐原版的跨会话串行消化、满批自动续跑、失败重试与游标续传、全局空闲尾批，以及 git 根目录兼容匹配、韩文检索回退、旧记忆版本目标解析、层级更新和用户手写记忆保留规则。先复现六类差异，再修复；新增会话不回填种子历史，已知会话按设置补消化。
