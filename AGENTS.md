# trisoul_x 工作约定

- 默认使用简体中文，先说结论，简洁直接。
- 最高设计原则：一切最小化、一切最简化，从核心开始构建。只加入当前真实需求必需的东西。
- 新版只有一个主执行模型，使用原生工具循环；不做多魂、投票、强制 JSON 回复或交稿封皮。
- 不添加产品安全门、审批流、强制验证流程、官位专属内部工具或其他非核心机制。
- 上下文与记忆是核心能力，围绕实际执行链路逐步接入；工具参数可以保留必要的 schema。
- 原 trisoul 项目只作实现参考。旧项目的契约、限制、兼容包袱和运行配置不自动继承。
- 优先直接、清晰的实现；不预建框架、多层抽象、多包体系或备用机制。不为尚未出现的需求扩展。
- UI 以对话为中心，只显示当前必要的信息和操作，避免堆积设置与监控面板。
- 提示词以原 trisoul 精心打磨的文本为底稿，优先原文融合与补充。只改不适配新架构的部分；所有改动必须记录到 PROMPT_CHANGES.md，逐项列原文、新文、类型与原因，交付时一并提供。
- 核心实现、疑难调试和重要验证由当前主模型负责；禁止选择或委派 Luna。只在明确需要且有授权时委派独立任务。
- 已授权工作自主推进，必要检查与改动风险相称；不要把开发阶段检查做成产品强制关卡。
- 修改前读取本文件。保持项目文档简短、与实际状态一致，不另建任务板或历史归档体系。
