# trisoul_x 工作约定

- 默认使用简体中文，先说结论，简洁直接。
- 最高设计原则：尽量保持原 trisoul 的提示词、数据结构和通用能力；最小化指架构适配和新增代码，不据此删减原版能力或规则。
- 2026-09-10 用户要求全面恢复与三魂无关的原有机制，不再逐项等待确认；既往明确审定的提示词删改、单主模型、原生工具和取消强制 JSON 回复继续有效。完整恢复工作文档、记忆补注/检索/整理、状态、压缩及其监控设置。
- 新版作为 DSH 插件运行，当前适配 DSH 0.1.5-rc.1；通用工具、技能、会话、文件交付与按需子代理复用宿主。
- 新版只有一个主执行模型，使用原生工具循环；不做多魂、投票、强制 JSON 回复或交稿封皮。按需子代理是保留的通用能力。
- 不添加未要求的产品安全门、审批流、官位专属内部工具或其他非核心机制；用户已明确要求恢复原版的三类任务收尾提醒。
- 上下文与记忆是核心能力，围绕实际执行链路逐步接入；工具参数可以保留必要的 schema。
- 记忆消化保留原 signals.overlap/conflict，并据此或已有记忆的实际变更触发整理；保留原整理规则及“the curation job cleans it up”原句，通过后台原生工具提交整理操作。
- 原 trisoul 项目保持只读；通用功能与提示词优先按原版迁移，只调整多魂、交稿/投票、强制 JSON 与新版 DSH 不适配的接口。运行配置保持独立。
- 优先直接、清晰的实现；不预建框架、多层抽象、多包体系或备用机制。不为尚未出现的需求扩展。
- Computer Use 以 Codex 的实际能力和使用体验为对标，质量优先。用户倾向插件整体重新实现；Vibex 用于经验与测试场景参考，默认不搬旧产品代码。不为省事保留旧模块，按适配、修错、验证和长期维护的整体成本判断；现有实现不作为能力上限。
- Computer Use 实现前先核全组件、用户流程与按平台／后端分列的能力基线；接口、UI、原生服务和配套依赖均须对账。资料冲突、未连接后端与未实测项不得当成已确认或无声删去；参照与验收见 COMPUTER_USE_BASELINE.md，不能只完成几个动作就声称完整对齐。
- Computer Use 新对话接手先读 COMPUTER_USE_HANDOFF.md，再读基线开头的用户确认范围及对应条目；历史实验按需查阅，不从头重做调研。用户计划以同一主模型的 High 推进，核心实现与重要验证仍由主模型负责；具体疑难再评估是否需要 Xhigh，不要求全程使用 Xhigh。文档不自动改变客户端模型设置。
- Computer Use 优先交付可实际使用的成果，再按真实任务暴露的问题和已确认缺项推进；保留必要回归、真实模型和重要风险验证，不为提速削弱断言或原有机制。已有可靠证据且相关代码未变时避免重复全量测试；不自行扩大范围，不把测试数量或未经核实的百分比当作完成度。
- 用户要求优先连续推进可直接编码、自动验证的工作，避免长时间占用电脑做来回 UI 操作；Codex 外观对照仍由助手负责。系统授权、日常扩展安装、真实应用试用及其他设备验收可交用户完成，提供简短步骤与预期结果；实现、调试和修错仍由主模型承担。人工验收不阻塞无依赖的开发，也不视为功能已通过。
- Chrome for Testing 的自动化夹具使用独立配置与测试钥匙串，不请求用户输入或授权真实钥匙串；真实登录态验收须单独明确目的，不能用测试钥匙串的通过结果代替。此前普通启动测试浏览器已触发用户钥匙串弹窗，不再重复该探测。
- 任务定义与完成状态用 todo_write 维护，验证链接与按需运行用独立 verify_link 工具；两者共用同一份任务记录和页面。保留原摘录/锚点、按项操作、完成状态与验证链接联动。主模型正常收尾时按原版依次提醒未完成任务、缺少合格证据、仅文字证据复核；文字证据成功复核后记录已问过，重新挂接的证据重新复核。
- BT（Better Todo）位于模型旁，按会话保存两个独立的收尾提醒开关：待办默认开，验证默认关。验证关闭时，待办提醒只列未完成项，不夹带证据要求；验证开关同时控制缺证据提醒与文字证据复核。两个开关不改变任务、验证工具和原主提示词。
- `todos` 是所有 preset 共享的宿主投影，须与 DSH 的 `todos@2` 注册版本及回合重置语义一致；X 的完整账本从事件独立恢复，必要时用 quiet 快照恢复任务栏，不改模型上下文。所有全局 X 钩子读取当前 `agentPreset` 投影，不能只读创建时的 header。安装测试须覆盖原版 web、两个挂载顺序、模式切换、原生工具、重启与卸载。
- 动态上下文注入前保留首个系统消息的位置；旧会话只修正一次，后续不得因该修复重排前缀或移动 DSH 支持的历史内系统更新。压缩遇到可识别的伪工具调用结果时保留原文；压力触发、手动压缩与消化条件保持原逻辑，不额外加限制。
- 2026-09-12 用户授权调整全部前端，采用 DSH 与 Codex 的融合风格：保留宿主布局与蓝色强调，统一中性色、紧凑工具栏及轻量预览；右侧 X 功能集中到工作台。常用信息优先，详细记录按需展开，兼顾浅色与深色主题，不要求整套像素复刻。
- UI 以对话为中心；设置、记忆、画布和监控面板按单模型插件架构重构和适配，复用 DSH 通用界面。
- 提示词以原 trisoul 精心打磨的文本为底稿，优先原文融合与补充。只改不适配新架构的部分；所有改动必须记录到 PROMPT_CHANGES.md，逐项列原文、新文、类型与原因，交付时一并提供。已审定：保留“用户不在场”原句；删除博识官的故障类比扩写和整个 output/REPLY_GUIDE；findings/plan/action 不迁入 note。
- 核心实现、疑难调试和重要验证由当前主模型负责；禁止选择或委派 Luna。只在明确需要且有授权时委派独立任务。
- 已授权工作自主推进，必要检查与改动风险相称；不要把开发阶段检查做成产品强制关卡。
- 修改前读取本文件。保持项目文档简短、与实际状态一致，不另建任务板或历史归档体系。
